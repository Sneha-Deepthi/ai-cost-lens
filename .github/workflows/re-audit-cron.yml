name: Reaudit Cron

on:
  schedule:
    - cron: "0 */6 * * *"

  workflow_dispatch:

jobs:
  trigger-reaudit:
    runs-on: ubuntu-latest

    steps:
      - name: Trigger pricing re-audit
        run: |
          curl -X POST https://ai-cost-lens-afrxxa971-sneha-deepthi-s-projects.vercel.app/api/detect-changes